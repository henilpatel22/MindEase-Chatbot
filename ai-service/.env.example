# MindEase AI Service — Environment Variables
PORT=5001
FLASK_DEBUG=true
# HuggingFace model to use for emotion detection
# Set to 'keyword' to use only the fast keyword-based fallback (no download required)
EMOTION_MODEL=j-hartmann/emotion-english-distilroberta-base
